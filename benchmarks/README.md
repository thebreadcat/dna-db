# Benchmarks

This directory contains benchmark suites and baseline results.

Planned Stage 1 benchmark targets:

- Write latency (p50/p95/p99)
- Write throughput under sustained load
- WAL replay duration after simulated crash
- Encoding/complement throughput (SIMD vs fallback)
