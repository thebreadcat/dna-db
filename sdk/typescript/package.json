{
  "name": "@dnadb/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for DNA-DB.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "integration": "npm run build && node scripts/integration.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
