[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dnadb-python-sdk"
version = "0.1.0"
description = "Python SDK for DNA-DB."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "DNA-DB Contributors" }]
dependencies = []

[tool.setuptools]
packages = ["dnadb"]
